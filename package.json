{
  "name": "lexicon",
  "version": "0.1.0",
  "description": "A modern, browser-based code editor inspired by Emacs",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bb dev",
    "build": "bb build",
    "build-dev": "bb build-dev",
    "build-wasm": "bb build-wasm",
    "build-grammars": "bb build-grammars",
    "test": "bb test",
    "clean": "bb clean",
    "serve": "bb serve",
    "build-and-serve": "bb build-and-serve",
    "dev-server": "bb dev-server"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}